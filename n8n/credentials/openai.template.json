{
  "_comment": "Plantilla de credencial OpenAI (LLM) para n8n. Crear esta credencial manualmente en la UI: Settings -> Credentials -> New -> OpenAI. Reemplazar el apiKey con el valor real de LLM_API_KEY (no commitear).",
  "name": "ExplorApp LLM",
  "type": "openAiApi",
  "data": {
    "apiKey": "${LLM_API_KEY}"
  }
}

{
  "_comment": "Plantilla de credencial PostgreSQL para n8n. Crear esta credencial manualmente en la UI: Settings -> Credentials -> New -> Postgres. Usar los valores indicados abajo (los reales se toman del .env).",
  "name": "ExplorApp Postgres",
  "type": "postgres",
  "data": {
    "host": "postgres",
    "database": "${POSTGRES_DB}",
    "user": "${POSTGRES_USER}",
    "password": "${POSTGRES_PASSWORD}",
    "port": 5432,
    "ssl": "disable"
  }
}
